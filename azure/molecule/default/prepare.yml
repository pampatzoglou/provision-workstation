---
- name: Prepare
  hosts: all
  tasks:
    - name: Update repositories cache and install "gpg" package
      ansible.builtin.apt:
        pkg:
          - gpg
        update_cache: true
        state: present
      become: true
